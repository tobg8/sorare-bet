import React from 'react';
import './styles.scss';

const TeamPreview = () => {
  const ok = 'truc';

  return (
    <div className="team-preview">
      teamPreview
    </div>
  );
};

export default TeamPreview;
